const StatCard = ({ title, value }) => (
    <div className="bg-white text-navy p-4 rounded-lg shadow-md">
      <h2 className="text-xl font-bold">{title}</h2>
      <p className="text-2xl">{value}</p>
    </div>
  );
  